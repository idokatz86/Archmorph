# Architecture Limitations Rule Library — Phase 1 seed (Issue #610).
#
# Each rule is evaluated by the engine in `backend/architecture_rules/engine.py`
# against an analysis result produced by `vision_analyzer.py`. A rule fires
# when its `predicate` returns a match; the resulting `ArchitectureIssue` is
# attached to the analysis under `architecture_issues[]`.
#
# Rule schema:
#   id              — stable kebab-case identifier (used by tests + UI)
#   title           — short human-readable headline
#   severity        — blocker | warning | info
#   category        — protocol | network-topology | sku-prereq | identity-auth
#                     | data-plane | region | tier-feature
#   message         — markdown body (multi-line OK)
#   remediation     — what to do instead
#   docs_url        — authoritative Microsoft Learn link
#   predicate       — name registered in predicates.py
#   predicate_args  — kwargs passed to the predicate
#   tags            — optional grouping
#
# Severity guidance:
#   blocker — generated IaC will be wrong / non-functional. IaC gate fires.
#   warning — works but has a significant gap.
#   info    — best-practice / awareness nudge.
#
# When you add a rule:
#   1. Pick or implement a predicate in predicates.py.
#   2. Author the card here.
#   3. Add at least one fixture-based unit test in
#      tests/test_architecture_rules.py asserting the rule fires (or doesn't).

rules:
  # ────────────────────────────────────────────────────────────
  # Category 1 — Protocol incompatibility
  # ────────────────────────────────────────────────────────────

  - id: front-door-protocol-mismatch
    title: Azure Front Door only supports HTTP/HTTPS traffic
    severity: blocker
    category: protocol
    message: |
      Azure Front Door is an HTTP/HTTPS Layer-7 service. It cannot proxy
      SFTP, SSH, FTP, SMTP, AMQP, MQTT, raw TCP, or any non-HTTP protocol.
      The architecture as drawn shows traffic crossing Front Door over a
      protocol it does not support — clients will not be able to reach
      the backend.
    remediation: |
      • For SFTP traffic to Azure Storage: bypass Front Door and connect
        the client directly to the storage account's SFTP endpoint. Use
        a Private Endpoint or NAT Gateway / firewall for IP allow-listing.
      • For raw TCP/UDP load balancing: use Standard Load Balancer.
      • For non-HTTP protocols at the edge: use Azure Firewall, an NVA,
        or expose the backend service directly with appropriate network
        controls.
    docs_url: https://learn.microsoft.com/azure/frontdoor/front-door-faq
    predicate: path_uses_service_with_protocol_mismatch
    predicate_args:
      via: Front Door
      allowed_protocols:
        - http
        - https
        - websocket
        - ws
        - wss
      disallowed_hint:
        - sftp
        - ssh
        - ftp
        - smtp
        - amqp
        - mqtt
    tags: [frontdoor, protocol, l7]

  - id: app-gateway-protocol-mismatch
    title: Azure Application Gateway is HTTP/HTTPS only (L7)
    severity: blocker
    category: protocol
    message: |
      Azure Application Gateway is a Layer-7 HTTP/HTTPS reverse proxy
      and Web Application Firewall. It does not handle SFTP, SSH, raw
      TCP/UDP, AMQP, MQTT, or other non-HTTP protocols. Routing those
      protocols through Application Gateway will fail.
    remediation: |
      • Use Standard Load Balancer for L4 (TCP/UDP) traffic.
      • Use Azure Firewall or an NVA for protocol-agnostic edge traffic.
      • For SFTP to Azure Storage, expose the storage SFTP endpoint
        directly (private endpoint + NSG/firewall recommended).
    docs_url: https://learn.microsoft.com/azure/application-gateway/overview
    predicate: path_uses_service_with_protocol_mismatch
    predicate_args:
      via: Application Gateway
      allowed_protocols:
        - http
        - https
        - websocket
        - ws
        - wss
      disallowed_hint:
        - sftp
        - ssh
        - ftp
        - smtp
        - amqp
        - mqtt
    tags: [appgateway, protocol, l7]

  - id: load-balancer-l4-only-info
    title: Azure Load Balancer is L4 — no HTTP routing or path rules
    severity: info
    category: protocol
    message: |
      Azure Standard Load Balancer operates at Layer 4 (TCP/UDP) only.
      It cannot inspect HTTP headers, perform path-based routing,
      terminate TLS at the application layer, or apply WAF rules.
      If your design relies on path/host routing, TLS termination, or
      Web Application Firewall, Standard Load Balancer is the wrong tool.
    remediation: |
      • For HTTP path/host routing → Application Gateway (regional) or
        Front Door (global).
      • For WAF protection → Application Gateway WAF SKU or
        Front Door Premium with WAF.
      • For TLS offload at L7 → Application Gateway / Front Door.
    docs_url: https://learn.microsoft.com/azure/load-balancer/load-balancer-overview
    predicate: service_present
    predicate_args:
      name: Load Balancer
    tags: [loadbalancer, l4, protocol]

  - id: front-door-sftp-storage-blocker
    title: Front Door cannot front Azure Storage SFTP — storage SFTP is SSH on port 22
    severity: blocker
    category: protocol
    message: |
      Azure Storage SFTP support exposes a Secure Shell File Transfer
      Protocol endpoint (TCP 22, SSH-based). Azure Front Door is an
      HTTP/HTTPS-only edge service and cannot proxy SSH/SFTP traffic.
      Placing Front Door in front of a storage account's SFTP endpoint
      means SFTP clients cannot reach the storage backend through it.
    remediation: |
      • Connect SFTP clients directly to the storage account's SFTP
        endpoint: ``<account>.blob.core.windows.net`` on TCP/22.
      • For network controls, use the storage account firewall, a
        Private Endpoint inside a VNet, or an Azure Firewall in front
        of the storage account — not Front Door.
      • If you also need HTTPS (Blob REST) on the same account, that
        path *can* go through Front Door; only SFTP traffic must
        bypass Front Door.
    docs_url: https://learn.microsoft.com/azure/storage/blobs/secure-file-transfer-protocol-support
    predicate: services_all_present
    predicate_args:
      names:
        - Front Door
        - SFTP
    tags: [frontdoor, storage, sftp, protocol]

  # ────────────────────────────────────────────────────────────
  # Category 2 — Network topology
  # ────────────────────────────────────────────────────────────

  - id: vnet-peering-non-transitive-warning
    title: VNet peering is not transitive — hub-spoke needs explicit routes
    severity: warning
    category: network-topology
    message: |
      Multiple Virtual Networks were detected. Azure VNet peering is
      **not transitive**: if VNet-A peers with VNet-B and VNet-B peers
      with VNet-C, traffic from A still cannot reach C without explicit
      routing through B.
      Hub-and-spoke designs must route spoke-to-spoke traffic through
      a Network Virtual Appliance, Azure Firewall, or VPN Gateway in
      the hub.
    remediation: |
      • Confirm whether your design needs spoke-to-spoke connectivity.
      • If yes, place an Azure Firewall (or NVA) in the hub and add
        User-Defined Routes on each spoke pointing to it.
      • Or use Azure Virtual WAN, which provides transitive routing.
    docs_url: https://learn.microsoft.com/azure/virtual-network/virtual-network-peering-overview
    predicate: service_count_at_least
    predicate_args:
      keywords:
        - virtual network
        - vnet
      threshold: 2
    tags: [vnet, peering, hub-spoke]

  - id: apim-consumption-tier-vnet-info
    title: API Management Consumption tier cannot join a VNet
    severity: info
    category: network-topology
    message: |
      Azure API Management was detected. The **Consumption** tier (the
      cheapest, serverless option) does *not* support VNet integration
      — there is no internal mode and no inbound private endpoint. If
      your APIs need to reach private backends (or be reachable only
      from inside a VNet), the Consumption tier is not viable.
    remediation: |
      • Choose Developer / Basic / Standard / Premium / Standard v2 for
        VNet-injected scenarios.
      • Premium and Standard v2 support both external and internal VNet
        modes; Developer is the cheapest tier with VNet support but is
        single-instance and not for production.
    docs_url: https://learn.microsoft.com/azure/api-management/api-management-features
    predicate: service_present
    predicate_args:
      name: API Management
    tags: [apim, vnet, sku]

  - id: functions-consumption-tier-vnet-info
    title: Functions Consumption plan cannot integrate with a VNet
    severity: info
    category: network-topology
    message: |
      Azure Functions on the **Consumption** plan does not support
      regional VNet integration or inbound private endpoints. If the
      function needs to reach a private database, key vault, or other
      service that is restricted to a VNet, the Consumption plan will
      not work.
    remediation: |
      • Use the **Flex Consumption** or **Premium** plan for VNet
        integration.
      • For inbound private access to the function, use the Premium
        or Dedicated plan plus a Private Endpoint.
    docs_url: https://learn.microsoft.com/azure/azure-functions/functions-networking-options
    predicate: service_present
    predicate_args:
      name: Azure Functions
    tags: [functions, vnet, sku]

  - id: container-apps-vnet-workload-profile-info
    title: Container Apps VNet integration requires a workload-profile environment
    severity: info
    category: network-topology
    message: |
      Azure Container Apps was detected. To deploy into a VNet, the
      Container Apps **Environment** must be created with a workload
      profile (or as an internal Consumption-only environment with VNet
      pre-provisioning). Existing environments cannot be retro-fitted
      with a VNet — it is set at creation time.
    remediation: |
      • Create the Environment with `--infrastructure-subnet-resource-id`
        and a workload profile if you need VNet integration.
      • If the Environment already exists without a VNet, plan a
        migration: stand up a new Environment, redeploy the apps,
        cut over the ingress.
    docs_url: https://learn.microsoft.com/azure/container-apps/networking
    predicate: service_present
    predicate_args:
      name: Container Apps
    tags: [containerapps, vnet, workload-profile]

  # ────────────────────────────────────────────────────────────
  # Category 3 — SKU prerequisites
  # ────────────────────────────────────────────────────────────

  - id: storage-sftp-requires-hns-info
    title: Storage SFTP requires Hierarchical Namespace (ADLS Gen2)
    severity: info
    category: sku-prereq
    message: |
      To enable the SFTP endpoint on an Azure Storage account, the
      account must have **Hierarchical Namespace** turned on (i.e., be
      a Data Lake Storage Gen2 account). HNS cannot be enabled after
      account creation. SFTP is also unavailable on Premium block-blob
      accounts in some configurations and requires the local-user
      authentication model.
    remediation: |
      • Create a new Standard general-purpose v2 storage account with
        `--enable-hierarchical-namespace true` from the start.
      • Configure local users (SFTP-specific) and key/password auth.
      • Migrate existing data from the old account if needed.
    docs_url: https://learn.microsoft.com/azure/storage/blobs/secure-file-transfer-protocol-support
    predicate: services_all_present
    predicate_args:
      names:
        - Storage
        - SFTP
    tags: [storage, sftp, hns]

  - id: front-door-private-link-premium-info
    title: Front Door Private Link origins require the Premium SKU
    severity: info
    category: sku-prereq
    message: |
      Azure Front Door + a Private Endpoint origin (e.g., a private
      App Service, Storage account, or Application Gateway) requires
      **Front Door Premium**. Standard SKU cannot connect to Private
      Link origins; only public-IP origins are supported.
    remediation: |
      • Upgrade to Front Door Premium if you need Private Link origins.
      • Or, if cost is a concern, expose the origin publicly with
        IP allow-listing keyed on Front Door's `X-Azure-FDID` header
        and the `AzureFrontDoor.Backend` service tag.
    docs_url: https://learn.microsoft.com/azure/frontdoor/private-link
    predicate: services_all_present
    predicate_args:
      names:
        - Front Door
        - Private
    tags: [frontdoor, privatelink, sku]

  - id: cosmos-db-free-tier-singleton-info
    title: Azure Cosmos DB free tier is one per subscription
    severity: info
    category: sku-prereq
    message: |
      Azure Cosmos DB free tier (1000 RU/s + 25 GB free) is limited
      to **one account per Azure subscription**. If a second Cosmos DB
      account is created in the same subscription, free tier cannot
      be applied to it.
    remediation: |
      • Decide which Cosmos account gets free tier and apply it
        deliberately.
      • For multi-environment work (dev/test/prod), put each in a
        separate subscription if you want each to get free tier.
      • Use serverless capacity for low-throughput workloads as an
        alternative cost-control mechanism.
    docs_url: https://learn.microsoft.com/azure/cosmos-db/free-tier
    predicate: service_present
    predicate_args:
      name: Cosmos DB
    tags: [cosmos, free-tier, sku]

  - id: app-service-deployment-slots-tier-info
    title: App Service deployment slots require Standard or higher
    severity: info
    category: sku-prereq
    message: |
      App Service **deployment slots** (used for blue/green deploys
      and slot swaps) are not available on Free, Shared, or Basic
      plans. Standard offers up to 5 slots, Premium offers 20.
    remediation: |
      • Use at minimum the Standard plan if you need slots.
      • Premium V3 is recommended for production VNet-integrated
        workloads with slot-based deployments.
    docs_url: https://learn.microsoft.com/azure/app-service/overview-hosting-plans
    predicate: service_present
    predicate_args:
      name: App Service
    tags: [appservice, slots, sku]

  # ────────────────────────────────────────────────────────────
  # Category 4 — Identity / auth gaps
  # ────────────────────────────────────────────────────────────

  - id: storage-account-key-vs-rbac-info
    title: Prefer Microsoft Entra (RBAC) over storage account keys
    severity: info
    category: identity-auth
    message: |
      Azure Storage was detected. Account keys grant full access to
      the storage account and cannot be scoped to a single container,
      blob, or operation. They are a frequent root cause of breach
      blast-radius incidents when leaked. Microsoft Entra
      authentication (RBAC + managed identity) is the recommended
      mechanism for modern workloads.
    remediation: |
      • Disable shared-key access on the storage account where
        feasible: ``az storage account update --allow-shared-key-access false``.
      • Use system-assigned or user-assigned managed identities on
        the calling services and assign the least-privilege built-in
        role (``Storage Blob Data Contributor``, ``Storage Blob Data Reader``, etc.).
      • Where SAS is unavoidable (browser uploads, partner integrations),
        prefer **user-delegation SAS** signed by an Entra identity.
    docs_url: https://learn.microsoft.com/azure/storage/common/authorize-data-access
    predicate: service_present
    predicate_args:
      name: Storage
    tags: [storage, identity, rbac]

  - id: cosmos-mi-data-plane-warning
    title: Cosmos DB data-plane RBAC needs explicit role assignment
    severity: warning
    category: identity-auth
    message: |
      Cosmos DB is detected. Managed identity for Cosmos DB
      **data-plane** access (read/write documents) is not granted by
      Reader / Contributor / Owner — those are control-plane only.
      You must assign one of the **Cosmos DB built-in data-plane
      roles** (e.g., ``Cosmos DB Built-in Data Reader`` or
      ``Cosmos DB Built-in Data Contributor``) using
      ``az cosmosdb sql role assignment create``.
      Forgetting this is a common cause of 401/403 at runtime even
      after the managed identity is correctly configured.
    remediation: |
      • Assign the Cosmos data-plane role to the application's
        managed identity. Example for an API:
        ``az cosmosdb sql role assignment create --account-name <acct> --resource-group <rg> --scope "/" --principal-id <objId> --role-definition-id 00000000-0000-0000-0000-000000000002``
      • In Bicep/Terraform, model the role assignment alongside the
        Cosmos account.
      • Disable account keys after MI is wired and verified.
    docs_url: https://learn.microsoft.com/azure/cosmos-db/how-to-setup-rbac
    predicate: service_present
    predicate_args:
      name: Cosmos DB
    tags: [cosmos, identity, rbac, data-plane]

  - id: keyvault-rbac-vs-policy-info
    title: Key Vault — prefer RBAC permission model over Access Policies
    severity: info
    category: identity-auth
    message: |
      Key Vault is detected. Microsoft now recommends the **Azure
      RBAC** permission model over the legacy access-policies model.
      Mixing the two within an organization is a common cause of
      access-denied errors after refactors.
    remediation: |
      • Create new Key Vaults with `--enable-rbac-authorization true`.
      • Assign built-in roles (``Key Vault Secrets User``,
        ``Key Vault Crypto User``, etc.) to managed identities.
      • Migrate existing access-policy vaults at a known maintenance
        window — the model is per-vault and cannot be partially
        applied.
    docs_url: https://learn.microsoft.com/azure/key-vault/general/rbac-guide
    predicate: service_present
    predicate_args:
      name: Key Vault
    tags: [keyvault, identity, rbac]

  # ────────────────────────────────────────────────────────────
  # Category 5 — Data plane / message limits
  # ────────────────────────────────────────────────────────────

  - id: service-bus-message-size-info
    title: Service Bus message size — 256 KB Standard, 100 MB Premium
    severity: info
    category: data-plane
    message: |
      Azure Service Bus enforces hard message-size limits:
      • **Standard** tier: 256 KB per message.
      • **Premium** tier: 100 MB per message.
      Workloads that need to send larger payloads via Service Bus
      Standard will fail with ``MessageSizeExceededException``. The
      common pattern for this is the **Claim Check** integration
      pattern: store the body in Blob Storage and send only a
      reference through Service Bus.
    remediation: |
      • Adopt the Claim Check pattern (Blob + Service Bus reference
        message).
      • Or upgrade the namespace to Premium if 100 MB is enough.
      • Compress payloads where appropriate.
    docs_url: https://learn.microsoft.com/azure/service-bus-messaging/service-bus-quotas
    predicate: service_present
    predicate_args:
      name: Service Bus
    tags: [servicebus, message-size, data-plane]

  - id: storage-queue-vs-service-bus-info
    title: Storage Queue is 64 KB / message — consider Service Bus for richer scenarios
    severity: info
    category: data-plane
    message: |
      Azure Storage Queue is detected. Storage Queue messages are
      capped at **64 KB** (48 KB if Base64-encoded), have no FIFO
      guarantee, no sessions, no transactions, and limited dead-letter
      semantics. For most application messaging needs, Azure Service
      Bus is the more capable choice.
    remediation: |
      • For simple, very-high-volume work-queue scenarios with small
        messages, Storage Queue is fine and cheap.
      • For ordered delivery, sessions, transactions, dead-lettering,
        topic/sub fan-out, or message sizes > 64 KB, switch to
        Service Bus (Queues for unicast, Topics + Subscriptions for
        fan-out).
    docs_url: https://learn.microsoft.com/azure/service-bus-messaging/service-bus-azure-and-service-bus-queues-compared-contrasted
    predicate: service_present
    predicate_args:
      name: Storage Queue
    tags: [storage-queue, servicebus, data-plane]

  - id: event-grid-without-deadletter-warning
    title: Event Grid without a dead-letter destination loses events after retries
    severity: warning
    category: data-plane
    message: |
      Event Grid is detected without a clearly co-located storage
      account or queue that could be used as a dead-letter target.
      Event Grid retries failed deliveries for up to **24 hours by
      default** (configurable on the subscription). After that,
      events that still cannot be delivered are dropped — unless a
      **dead-letter destination** (Blob Storage container) is
      configured on the subscription.
    remediation: |
      • Provision a dedicated Storage Account container for
        dead-lettering and configure it on every Event Grid
        subscription that handles business-critical events.
      • Build alerting on dead-letter blob count > 0.
      • Document the retention policy of the DLQ container.
    docs_url: https://learn.microsoft.com/azure/event-grid/manage-event-delivery
    predicate: category_present_without_companion
    predicate_args:
      category: integration
      companions:
        - Storage
        - Blob
    tags: [eventgrid, dlq, data-plane]

  # ────────────────────────────────────────────────────────────
  # Category 6 — Region / sovereignty
  # ────────────────────────────────────────────────────────────

  - id: openai-region-availability-info
    title: Azure OpenAI is region- and model-restricted — verify availability
    severity: info
    category: region
    message: |
      Azure OpenAI Service was detected. Different models, deployment
      types (Standard / Provisioned / Global), and capacity tiers are
      available in different regions. A model your code calls today
      may not be deployable in your preferred region tomorrow.
    remediation: |
      • Check region & model availability before pinning a deployment
        region (and revisit before contract renewal): Microsoft Learn
        publishes a regularly-updated availability matrix.
      • For latency-sensitive workloads, use the **Standard Global**
        deployment type, which routes to capacity worldwide.
      • For PTU (provisioned throughput), confirm the region has
        capacity *before* committing.
    docs_url: https://learn.microsoft.com/azure/ai-services/openai/concepts/models
    predicate: service_present
    predicate_args:
      name: OpenAI
    tags: [openai, region, model-availability]

  - id: confidential-compute-region-info
    title: Confidential Computing SKUs are limited to specific regions
    severity: info
    category: region
    message: |
      Azure Confidential Computing (Confidential VMs, Confidential
      Containers on AKS, DCsv3/ECsv5 SKUs) is available only in a
      subset of Azure regions and only on specific VM families. If
      your design requires confidential compute, lock down the region
      decision early.
    remediation: |
      • Verify region availability for the specific SKU you plan to
        use (DCsv3, ECsv5, etc.) on Microsoft Learn.
      • For AKS Confidential Containers, also verify the AKS
        Confidential Containers preview/GA status in your region.
    docs_url: https://learn.microsoft.com/azure/confidential-computing/overview-azure-products
    predicate: service_present
    predicate_args:
      name: Confidential
    tags: [confidential-computing, region, sku]

  - id: azure-government-feature-gap-info
    title: Azure Government has feature & service-availability gaps vs commercial
    severity: info
    category: region
    message: |
      Azure Government is mentioned. Azure Government clouds (US Gov
      Virginia, Arizona, Texas — and the equivalent China /
      sovereign clouds) lag the commercial cloud on **service
      availability** and **feature flags** by weeks-to-months. Many
      services that ship in commercial are not available, or are
      available only in older API versions, in Government clouds.
    remediation: |
      • Cross-check every service in this design against the Azure
        Government services-by-region matrix before committing.
      • For services that exist in both, verify the API version and
        regional endpoint match what your code expects (the endpoint
        suffix differs from commercial: ``.usgovcloudapi.net``).
    docs_url: https://learn.microsoft.com/azure/azure-government/documentation-government-services-supportedservices
    predicate: service_present
    predicate_args:
      name: Government
    tags: [government, sovereign, region]

  # ────────────────────────────────────────────────────────────
  # Category 7 — Tier feature dependencies
  # ────────────────────────────────────────────────────────────

  - id: sql-serverless-no-read-replicas-info
    title: Azure SQL Serverless has no read-only replicas
    severity: info
    category: tier-feature
    message: |
      Azure SQL Database is detected. The **Serverless** compute tier
      (Gen5 only) does not support read-only secondary replicas the
      way Provisioned Hyperscale and Business Critical do. Workloads
      that depend on offloading read traffic to a replica cannot use
      Serverless without redesign.
    remediation: |
      • If read-replica offload is essential, use **Hyperscale**
        (named replicas) or **Business Critical** (built-in HA
        replicas + read-scale).
      • If autopause is the main reason for picking Serverless,
        consider Hyperscale's pause-resume capability instead.
    docs_url: https://learn.microsoft.com/azure/azure-sql/database/serverless-tier-overview
    predicate: service_present
    predicate_args:
      name: SQL Database
    tags: [sql, serverless, replicas, tier]

  - id: front-door-custom-domain-cert-tier-info
    title: Front Door managed certificates require Standard or Premium SKU
    severity: info
    category: tier-feature
    message: |
      Front Door is detected. **Azure-managed TLS certificates** for
      custom domains are available only on Front Door Standard /
      Premium. The classic / Basic tier requires you to bring your
      own certificate and manage rotation yourself.
    remediation: |
      • Use Front Door Standard or Premium and let Azure issue and
        rotate certs automatically.
      • If you must stay on classic, integrate with Key Vault and
        automate cert renewal via a pipeline.
    docs_url: https://learn.microsoft.com/azure/frontdoor/standard-premium/how-to-configure-https-custom-domain
    predicate: service_present
    predicate_args:
      name: Front Door
    tags: [frontdoor, tls, tier]

  - id: functions-private-endpoint-tier-warning
    title: Function Apps that consume private services should be on Premium / Flex Consumption
    severity: warning
    category: tier-feature
    message: |
      Azure Functions is detected alongside services typically used
      with Private Endpoints (Storage, Cosmos DB, Service Bus, SQL).
      The default **Consumption** plan cannot integrate with a VNet
      and therefore cannot reach those private endpoints. Designs
      that pair Functions with private services almost always need
      **Flex Consumption** or **Premium**.
    remediation: |
      • Use the Flex Consumption plan if available in your region
        (newer, cheaper than Premium for many workloads).
      • Otherwise use the Premium plan with regional VNet integration.
      • For pure public ingestion with no backend access, Consumption
        is fine — but rare.
    docs_url: https://learn.microsoft.com/azure/azure-functions/flex-consumption-plan
    predicate: services_all_present
    predicate_args:
      names:
        - Azure Functions
        - Private
    tags: [functions, vnet, tier]

  - id: service-bus-sessions-tier-info
    title: Service Bus message sessions require Standard or Premium
    severity: info
    category: tier-feature
    message: |
      Service Bus is detected. **Message sessions** (FIFO ordering
      per session-id, request-response correlation, etc.) are
      available on Standard and Premium tiers but not on the Basic
      tier. Most application-level messaging patterns that rely on
      ordering or correlation need sessions.
    remediation: |
      • Use at least the Standard tier if your application requires
        sessions, transactions, topics & subscriptions, or auto-forward.
      • Premium adds resource isolation, larger messages (100 MB),
        and predictable latency.
    docs_url: https://learn.microsoft.com/azure/service-bus-messaging/message-sessions
    predicate: service_present
    predicate_args:
      name: Service Bus
    tags: [servicebus, sessions, tier]

  # ────────────────────────────────────────────────────────────
  # Category 8 — Resilience / security / governance posture
  # ────────────────────────────────────────────────────────────

  - id: dr-cross-region-replication-warning
    title: Stateful workloads need explicit cross-region recovery posture
    severity: warning
    category: resilience
    message: |
      Stateful services were detected without clear evidence of a
      cross-region recovery pattern. Zone redundancy protects against
      datacenter failure, but it does not by itself cover regional outage,
      accidental deletion, or region-scoped service disruption.
    remediation: |
      Add explicit disaster-recovery evidence such as SQL failover groups,
      Cosmos DB multi-region writes, geo-redundant storage, Azure Site
      Recovery, paired-region deployment, or Traffic Manager / Front Door
      failover routing. Document RPO/RTO targets with the chosen pattern.
    docs_url: https://learn.microsoft.com/azure/reliability/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - SQL Database
        - PostgreSQL
        - MySQL
        - Cosmos DB
        - Storage
        - Blob
        - Database
      companions:
        - Failover Group
        - Geo-redundant
        - GRS
        - RA-GRS
        - Cross-region
        - Paired Region
        - Traffic Manager
        - Site Recovery
        - Multi-region
      companion_mode: coverage
    tags: [resilience, disaster-recovery, replication]

  - id: edge-security-waf-ddos-warning
    title: Internet-facing entry points need WAF or DDoS posture
    severity: warning
    category: security
    message: |
      Internet-facing entry services were detected without a clear WAF,
      Azure Firewall, or DDoS protection signal. Public HTTP entry points
      should declare where request inspection, managed rules, rate controls,
      and volumetric protection are enforced.
    remediation: |
      Add Azure Web Application Firewall on Front Door or Application Gateway
      for HTTP workloads. For network-layer exposure, document Azure DDoS
      Network Protection, Azure Firewall, or an equivalent NVA pattern.
    docs_url: https://learn.microsoft.com/azure/web-application-firewall/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Front Door
        - Application Gateway
        - Public IP
        - CDN
        - Internet
        - External Endpoint
      companions:
        - WAF
        - Web Application Firewall
        - DDoS
        - Azure Firewall
        - Firewall Policy
    tags: [edge, waf, ddos, security]

  - id: identity-layer-missing-warning
    title: Application workloads should declare an identity layer
    severity: warning
    category: identity-auth
    message: |
      Application workloads were detected without visible Microsoft Entra ID,
      managed identity, or Key Vault integration. Without an explicit identity
      layer, generated deployments often drift toward shared keys, embedded
      secrets, or ad-hoc authentication.
    remediation: |
      Add Microsoft Entra ID for user and workload identity, enable managed
      identities on compute resources, and store secrets/certificates in Key
      Vault with least-privilege RBAC assignments.
    docs_url: https://learn.microsoft.com/entra/identity/managed-identities-azure-resources/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - App Service
        - Container Apps
        - AKS
        - Kubernetes
        - Azure Functions
        - API Management
        - Virtual Machine
      companions:
        - Entra
        - Azure AD
        - Microsoft Entra
        - Managed Identity
        - Key Vault
        - Workload Identity
      companion_mode: coverage
    tags: [identity, managed-identity, entra]

  - id: backup-posture-missing-warning
    title: Data services need an explicit backup and restore posture
    severity: warning
    category: resilience
    message: |
      Data-bearing services were detected without backup or restore evidence.
      Replication is not a substitute for recoverable backups: operator error,
      ransomware, bad migrations, and destructive application bugs can be
      replicated quickly across healthy infrastructure.
    remediation: |
      Document Azure Backup, Recovery Services Vault / Backup Vault, database
      point-in-time restore, immutable backup, retention period, and restore
      test cadence for every stateful service.
    docs_url: https://learn.microsoft.com/azure/backup/backup-overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - SQL Database
        - PostgreSQL
        - MySQL
        - Cosmos DB
        - Storage
        - Blob
        - Database
        - Virtual Machine
      companions:
        - Backup
        - Recovery Services Vault
        - Backup Vault
        - Point-in-time Restore
        - PITR
        - Immutable Backup
        - Snapshot
      exclude_keywords:
        - Failover Group
        - Geo-redundant
      companion_mode: coverage
    tags: [backup, restore, resilience]

  - id: tagging-policy-missing-info
    title: Multi-service designs should include tagging or policy governance
    severity: info
    category: governance
    message: |
      A multi-service architecture was detected without tagging or Azure Policy
      evidence. Missing ownership, environment, cost-center, and data-class tags
      make cost allocation, incident response, and compliance reporting harder
      once the design is deployed.
    remediation: |
      Add an Azure Policy or landing-zone governance component that enforces
      required tags such as owner, environment, costCenter, application, and
      dataClassification. Apply inheritance at management group, subscription,
      or resource-group scope where appropriate.
    docs_url: https://learn.microsoft.com/azure/azure-resource-manager/management/tag-resources
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - App Service
        - Container Apps
        - Front Door
        - Application Gateway
        - SQL Database
        - PostgreSQL
        - MySQL
        - Cosmos DB
        - Storage
        - Service Bus
        - Key Vault
        - Virtual Network
      companions:
        - Tagging
        - Tags
        - Azure Policy
        - Management Group
        - Cost Management
        - Resource Graph
      threshold: 3
    tags: [tagging, governance, policy]

  # ────────────────────────────────────────────────────────────
  # Category 9 — Phase 3 posture expansion (#662)
  # ────────────────────────────────────────────────────────────

  - id: public-data-service-without-private-link-warning
    title: Data services should show Private Link or network isolation
    severity: warning
    category: network-topology
    message: |
      Data-bearing services were detected without visible Private Link,
      private endpoint, VNet, service endpoint, or firewall isolation evidence.
      Public data-plane exposure increases exfiltration risk and makes it
      harder to prove least-privilege network access during migration review.
    remediation: |
      Add Private Endpoints or service endpoints for databases, storage, and
      vaults where possible. Document firewall rules, trusted service bypass,
      DNS resolution, and any intentionally public endpoint exceptions.
    docs_url: https://learn.microsoft.com/azure/private-link/private-link-overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - SQL Database
        - PostgreSQL
        - MySQL
        - Cosmos DB
        - Storage
        - Blob
        - Key Vault
      companions:
        - Private Endpoint
        - Private Link
        - Service Endpoint
        - Virtual Network
        - VNet
        - Firewall
    tags: [privatelink, data-plane, network-isolation]

  - id: workload-secrets-without-key-vault-warning
    title: Workloads should externalize secrets through Key Vault or workload identity
    severity: warning
    category: identity-auth
    message: |
      Application workloads were detected without a clear managed secret store,
      managed identity, or workload identity signal. This often leads to
      environment variables, shared keys, or repository-stored credentials
      becoming the implicit secret-management design.
    remediation: |
      Use Azure Key Vault with managed identity or workload identity for every
      compute workload. Store certificates, connection strings, API keys, and
      rotation metadata outside application code and deployment templates.
    docs_url: https://learn.microsoft.com/azure/key-vault/general/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - App Service
        - Container Apps
        - AKS
        - Kubernetes
        - Azure Functions
        - Virtual Machine
        - API Management
      companions:
        - Key Vault
        - Managed Identity
        - Workload Identity
        - Microsoft Entra
        - Entra
        - Secret Store
    tags: [secrets, keyvault, managed-identity]

  - id: observability-anchor-missing-warning
    title: Production workloads need an observability anchor
    severity: warning
    category: operations
    message: |
      Production workload services were detected without a visible telemetry,
      logging, alerting, or tracing anchor. Without a declared observability
      path, incident response depends on ad-hoc portal checks and post-failure
      instrumentation.
    remediation: |
      Add Application Insights, Azure Monitor, Log Analytics, OpenTelemetry,
      managed Prometheus/Grafana, or an approved third-party telemetry stack.
      Include alerts for availability, saturation, errors, and dependency
      failures before production cutover.
    docs_url: https://learn.microsoft.com/azure/azure-monitor/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - App Service
        - Container Apps
        - AKS
        - Kubernetes
        - Azure Functions
        - API Management
        - SQL Database
        - Service Bus
        - Event Hubs
      companions:
        - Application Insights
        - Azure Monitor
        - Log Analytics
        - OpenTelemetry
        - Grafana
        - Prometheus
        - Alert
        - Workbook
        - Datadog
        - Splunk
    tags: [observability, monitoring, alerts]

  - id: observability-stack-overlap-info
    title: Multiple observability stacks need clear ownership boundaries
    severity: info
    category: operations
    message: |
      Several observability tools were detected. Multiple telemetry stacks can
      be valid, but without ownership boundaries they commonly duplicate
      ingestion cost, split incident response, and create competing alert rules.
    remediation: |
      Document the system of record for metrics, logs, traces, dashboards, and
      paging. Route shared signals through one primary workspace or event path
      and define which team owns each alert policy.
    docs_url: https://learn.microsoft.com/azure/azure-monitor/best-practices
    predicate: service_count_at_least
    predicate_args:
      keywords:
        - Application Insights
        - Azure Monitor
        - Log Analytics
        - Sentinel
        - Grafana
        - Prometheus
        - Datadog
        - Splunk
      threshold: 3
    tags: [observability, cost, operations]

  - id: regulated-workload-without-policy-anchor-warning
    title: Regulated workloads need compliance and audit anchors
    severity: warning
    category: governance
    message: |
      Regulated workload signals were detected without clear Azure Policy,
      Defender, Purview, Sentinel, Key Vault, private endpoint, or immutable
      audit evidence. Regulated migrations need controls visible in the target
      architecture, not only in deployment runbooks.
    remediation: |
      Add compliance boundaries such as Azure Policy initiatives, Defender for
      Cloud recommendations, Purview data governance, Sentinel audit routing,
      Key Vault, Private Link, and immutable log or storage retention.
    docs_url: https://learn.microsoft.com/azure/governance/policy/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - HIPAA
        - PCI
        - FedRAMP
        - PHI
        - PII
        - Government
        - Confidential
        - Healthcare
        - Financial
      companions:
        - Azure Policy
        - Defender
        - Purview
        - Sentinel
        - Key Vault
        - Private Endpoint
        - Immutable
        - Audit
    tags: [regulated, compliance, audit]

  - id: multi-domain-edge-validation-info
    title: Multi-domain edge designs need DNS and certificate validation ownership
    severity: info
    category: network-topology
    message: |
      Domain, DNS, certificate, and edge-service signals were detected without
      explicit validation or certificate-rotation ownership. Multi-domain edge
      migrations often stall on DNS proof, SAN/wildcard constraints, or manual
      certificate renewal paths.
    remediation: |
      Document DNS ownership validation, managed certificate eligibility,
      wildcard/SAN limits, Key Vault-backed certificate rotation, and the team
      responsible for domain cutover approvals.
    docs_url: https://learn.microsoft.com/azure/frontdoor/standard-premium/how-to-add-custom-domain
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Custom Domain
        - DNS Zone
        - Certificate
        - Front Door
        - Application Gateway
      companions:
        - DNS Validation
        - Managed Certificate
        - Certificate Rotation
        - Key Vault
      threshold: 3
    tags: [dns, certificate, edge]

  - id: multi-tenant-app-without-tenant-isolation-warning
    title: Multi-tenant apps need an explicit tenant-isolation model
    severity: warning
    category: identity-auth
    message: |
      Multi-tenant or SaaS workload signals were detected without tenant
      isolation, external identity, partitioning, or per-tenant boundary
      evidence. Missing tenant boundaries make data leakage, noisy-neighbor
      behavior, and support escalations much harder to contain.
    remediation: |
      Define the tenancy model explicitly: pooled, siloed, or hybrid. Add
      Entra External ID/B2C where appropriate, tenant-aware partition keys,
      row-level security, per-tenant encryption or resource boundaries, and
      tenant-scoped observability dimensions.
    docs_url: https://learn.microsoft.com/azure/architecture/guide/multitenant/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Multi-tenant
        - SaaS
        - Tenant
        - Customer Portal
      companions:
        - Tenant Isolation
        - Entra External ID
        - B2C
        - Partition Key
        - Row-level Security
        - Elastic Pool
        - Per-tenant
    tags: [multitenant, saas, isolation]

  - id: service-bus-without-dead-letter-operations-warning
    title: Service Bus workloads need dead-letter operations
    severity: warning
    category: data-plane
    message: |
      Service Bus queues, topics, or subscriptions were detected without a
      dead-letter handling signal. Dead-letter queues are only useful when the
      architecture includes monitoring, triage ownership, replay, and poison
      message handling.
    remediation: |
      Configure dead-letter monitoring and alerts, define replay/runbook
      ownership, track max delivery count, and make poison-message handling a
      first-class operational workflow before cutover.
    docs_url: https://learn.microsoft.com/azure/service-bus-messaging/service-bus-dead-letter-queues
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Service Bus
        - Queue
        - Topic
        - Subscription
      companions:
        - Dead Letter
        - DLQ
        - Poison Queue
        - Replay
      exclude_keywords:
        - Storage Queue
    tags: [servicebus, dlq, operations]

  - id: event-hubs-without-checkpoint-storage-warning
    title: Event Hubs consumers need durable checkpointing
    severity: warning
    category: data-plane
    message: |
      Event Hubs was detected without a checkpoint, consumer-group, or offset
      store signal. Event Processor clients need durable checkpointing to resume
      consumption predictably after restarts, scale-out, or failover.
    remediation: |
      Add Blob Storage-backed checkpointing, define consumer groups per
      application boundary, monitor lag, and document replay behavior for
      poison or out-of-order event handling.
    docs_url: https://learn.microsoft.com/azure/event-hubs/event-processor-balance-partition-load
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Event Hubs
        - Event Hub
      companions:
        - Checkpoint
        - Consumer Group
        - Offset Store
        - Blob Lease
    tags: [eventhubs, checkpoint, streaming]

  - id: public-api-without-rate-limiting-warning
    title: Public APIs need rate limiting or request controls
    severity: warning
    category: security
    message: |
      Public API entry points were detected without rate limiting, quota,
      throttling, WAF policy, or cache-based request control evidence. Public
      endpoints without request controls are vulnerable to abuse, cost spikes,
      and noisy-neighbor traffic.
    remediation: |
      Add API Management rate-limit/quota policies, Front Door or Application
      Gateway WAF controls, application throttling, or Redis-backed request
      counters. Document expected burst, sustained RPS, and client quotas.
    docs_url: https://learn.microsoft.com/azure/api-management/rate-limit-policy
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - API Management
        - API Gateway
        - Public API
        - Front Door
        - Application Gateway
        - App Service
        - Azure Functions
      companions:
        - Rate Limit
        - Throttling
        - Quota
        - WAF
        - APIM Policy
        - Redis
    tags: [api, rate-limit, security]

  - id: zone-redundancy-posture-missing-warning
    title: Zone-capable production services need zone redundancy decisions
    severity: warning
    category: resilience
    message: |
      Zone-capable production services were detected without availability zone,
      zone-redundant, or ZRS evidence. Region-local resilience depends on an
      explicit zone posture; default single-zone or non-zonal deployments can
      become hidden availability risks.
    remediation: |
      Decide per service whether to use zone redundancy, zonal pinning, or a
      documented exception. Capture ZRS storage, zone-redundant gateways,
      zone-balanced AKS/node pools, and database HA tier choices in the design.
    docs_url: https://learn.microsoft.com/azure/reliability/availability-zones-overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Application Gateway
        - Azure Firewall
        - AKS
        - Kubernetes
        - App Service
        - Container Apps
        - SQL Database
        - PostgreSQL
        - Storage
        - Redis
        - Cosmos DB
      companions:
        - Availability Zone
        - Availability Zones
        - Zone Redundant
        - Zone-redundant
        - ZRS
        - AZ
    tags: [zones, resilience, ha]

  - id: premium-edge-cost-acknowledgement-info
    title: Private-origin Front Door designs should acknowledge Premium edge cost
    severity: info
    category: cost
    message: |
      Front Door with private endpoint or Private Link origin evidence was
      detected. That architecture normally requires Front Door Premium, which
      can be the right choice but should be an explicit cost and capability
      decision rather than an incidental dependency.
    remediation: |
      Confirm traffic volume, WAF requirements, private-origin value, and
      monthly edge cost before committing. If private origin is not required,
      compare Standard Front Door with origin hardening using service tags,
      FDID headers, and application-level access controls.
    docs_url: https://learn.microsoft.com/azure/frontdoor/private-link
    predicate: services_all_present
    predicate_args:
      names:
        - Front Door
        - Private Endpoint
    tags: [frontdoor, cost, premium]

  # ────────────────────────────────────────────────────────────
  # Category 10 — ActOne/XSE engine + protocol mapping decisions (#1029)
  # ────────────────────────────────────────────────────────────

  - id: sftp-s3-ingestion-decision-blocker
    title: SFTP + S3 ingestion workflows need explicit Azure ingestion controls
    severity: blocker
    category: data-plane
    message: |
      An SFTP + S3-style incoming-bucket workflow was detected without explicit
      ingestion-path controls. Production migration needs a declared target
      pattern (Azure Storage/ADLS Gen2 SFTP), private access, firewall/auth
      controls, lifecycle policy, and downstream trigger handling.
    remediation: |
      Choose and document one ingestion path: Azure Blob/ADLS Gen2 SFTP endpoint
      with local users or approved identity model, private endpoint/firewall
      controls, lifecycle/retention policy, and trigger wiring (Event Grid,
      Data Factory, Function, or Logic App) for downstream processing.
    docs_url: https://learn.microsoft.com/azure/storage/blobs/secure-file-transfer-protocol-support
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - SFTP
        - S3
        - Incoming Bucket
      companions:
        - ADLS Gen2
        - Azure Storage SFTP
        - Private Endpoint
        - Storage Firewall
        - Local User
        - Lifecycle
        - Event Grid
        - Data Factory
        - Logic App
        - Function Trigger
      threshold: 2
    tags: [sftp, s3, ingestion, storage, blockers]

  - id: appstream-target-decision-blocker
    title: AppStream workloads require explicit Azure Virtual Desktop or modernization choice
    severity: blocker
    category: identity-auth
    message: |
      AppStream usage was detected without an explicit target decision. Production
      planning must choose Azure Virtual Desktop RemoteApp (or a modernization
      path) with identity, private access, and session profile assumptions.
    remediation: |
      Select one target path: (1) Azure Virtual Desktop RemoteApp, (2) app
      modernization to web/native, or (3) managed third-party VDI. Capture Entra
      identity integration, network isolation/private access, and session-data
      storage assumptions before generating production IaC.
    docs_url: https://learn.microsoft.com/azure/virtual-desktop/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - AppStream
      companions:
        - Azure Virtual Desktop
        - RemoteApp
        - Modernization
        - Entra
        - Private Endpoint
        - Private Access
    tags: [appstream, avd, vdi, modernization]

  - id: alb-l7-ingress-decision-blocker
    title: ALB ingress needs Application Gateway WAF v2 listener/backend mapping decisions
    severity: blocker
    category: network-topology
    message: |
      ALB ingress was detected without explicit Application Gateway WAF v2
      listener and backend-pool mapping details. For production migrations,
      separate listener/backend intents (for example AVA vs ActOne paths) must
      be preserved rather than collapsed into a generic edge mapping.
    remediation: |
      Define a regional Application Gateway WAF v2 design with explicit HTTPS
      listeners, routing rules, and backend pools that preserve each source ALB
      ingress path. Document certificate/WAF policy ownership and rule mapping.
    docs_url: https://learn.microsoft.com/azure/application-gateway/overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - ALB
        - Application Load Balancer
      companions:
        - WAF v2
        - Listener
        - Backend Pool
        - Routing Rule
      threshold: 1
    tags: [alb, appgateway, waf, ingress]

  - id: rds-engine-identification-blocker
    title: Generic RDS mappings need engine identification before Azure target selection
    severity: blocker
    category: data-plane
    message: |
      Generic RDS was detected without engine identification. Mapping RDS
      directly to Azure SQL by default can be incorrect for PostgreSQL/MySQL and
      hides compatibility, operational, and licensing decisions.
    remediation: |
      Identify the RDS engine first (PostgreSQL, MySQL, SQL Server, MariaDB,
      Oracle, etc.) and only then choose the Azure target. If engine is unknown,
      keep the mapping unresolved and block production IaC until clarified.
    docs_url: https://learn.microsoft.com/azure/azure-sql/migration-guides/
    predicate: rds_engine_unresolved
    tags: [rds, engine, database, blockers]

  - id: kafka-compatibility-path-blocker
    title: Kafka/MSK workloads require explicit compatibility target path
    severity: blocker
    category: data-plane
    message: |
      Kafka/MSK signals were detected without a compatibility target decision.
      Production migration must choose between Event Hubs Kafka endpoint,
      Confluent Cloud, or self-managed Kafka on AKS based on client/protocol
      compatibility needs.
    remediation: |
      Document the Kafka target option and compatibility assumptions: protocol
      version/client expectations, schema registry approach, connector strategy,
      and operational ownership.
    docs_url: https://learn.microsoft.com/azure/event-hubs/azure-event-hubs-apache-kafka-overview
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Kafka
        - MSK
      companions:
        - Confluent
        - Kafka on AKS
        - Self-managed Kafka
        - Schema Registry
        - Kafka Compatibility
      threshold: 1
    tags: [kafka, msk, streaming, blockers]

  - id: efs-filesystem-choice-blocker
    title: EFS-backed workloads need filesystem target choice (Azure Files Premium NFS vs ANF)
    severity: blocker
    category: data-plane
    message: |
      EFS-style shared filesystem usage was detected without a target filesystem
      decision. Production migration should choose Azure Files Premium NFS or
      Azure NetApp Files based on POSIX behavior and performance requirements.
    remediation: |
      Capture filesystem requirements (POSIX semantics, locking, throughput,
      latency, and scale), then choose Azure Files Premium NFS or Azure NetApp
      Files explicitly before generating production IaC.
    docs_url: https://learn.microsoft.com/azure/storage/files/files-nfs-protocol
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - EFS
      companions:
        - NetApp Volume
        - POSIX
        - NFSv4.1
        - Throughput
        - Latency
    tags: [efs, nfs, posix, storage, blockers]

  - id: elasticsearch-workload-classification-blocker
    title: Elasticsearch/OpenSearch mappings need workload classification
    severity: blocker
    category: operations
    message: |
      Elasticsearch/OpenSearch was detected without clear workload
      classification (logs, business metrics, or product search). A generic
      search mapping can misplace log analytics workloads.
    remediation: |
      Classify Elasticsearch usage first: log analytics, business metrics, or
      product/content search. Use Azure Monitor/Log Analytics/Data Explorer for
      logs and operational analytics; reserve Azure AI Search for search/RAG
      use-cases where appropriate.
    docs_url: https://learn.microsoft.com/azure/architecture/data-guide/technology-choices/search-options
    predicate: service_keywords_without_companion
    predicate_args:
      keywords:
        - Elasticsearch
        - ElasticSearch
        - OpenSearch
      companions:
        - Log Analytics
        - Azure Monitor
        - Data Explorer
        - Business Metrics
        - Product Search
        - Full-text Search
      threshold: 1
    tags: [elasticsearch, opensearch, logs, metrics, search]

  - id: active-active-traffic-split-ambiguity-warning
    title: Active-active label conflicts with failover-style 100/0 traffic split
    severity: warning
    category: resilience
    message: |
      The architecture signals active-active while traffic appears 100/0
      failover-style. This is ambiguous and can lead to incorrect HA/DR plans.
    remediation: |
      Clarify whether the workload is true active-active or active-passive
      failover. Confirm expected steady-state traffic split, RTO, RPO, and
      failover orchestration/runbook assumptions before production cutover.
    docs_url: https://learn.microsoft.com/azure/architecture/framework/resiliency/reliability-pillars
    predicate: active_active_with_failover_traffic_split
    tags: [ha, dr, active-active, failover, rto, rpo]
